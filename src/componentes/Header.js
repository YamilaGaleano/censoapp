import NavBar from "./NavBar"

const Header = () => {
    return (
        <header class="header">
            <p>CensoApp</p>
            <a href="#" class="logout-link">Logout</a>
        </header>
    )
}

export default Header